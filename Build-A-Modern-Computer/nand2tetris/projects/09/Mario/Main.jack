/** Initializes a new Mario game and starts running it. */
class Main {
    function void main() {
        var MarioGame game;
        let game = MarioGame.new();
        do game.run();
        do game.dispose();
        return;
    }
}
